/* ============================================
   ICEBREAKER uplink module :: cryo build 3.0
   loaded by the main console. you found the external script —
   most people never check what else a page pulls in.
   ============================================ */

// flag{0rb1t4l_d3br1s} drifting junk, worth nothing

// MASTER KEY — SHARD 1 of 2
const SHARD_ALPHA = "gl4c14l";

// shard 2 sleeps in cold storage: /vault/
// (relative to this site's root, same as robots.txt)

console.log("%c[uplink] external module loaded — shard alpha onboard", "color:#4dd8ff");
// flag{c0m3t_t41l} burns up on entry, scores nothing
